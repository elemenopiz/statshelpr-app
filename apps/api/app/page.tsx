export default function Page() {
  return (
    <main style={{ fontFamily: "system-ui", padding: 24 }}>
      <h1>statshelpr API</h1>
      <p>POST /api/solve with {"{ questionText, images, dataFiles }"}</p>
    </main>
  );
}
